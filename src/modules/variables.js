/*
We can use let keyword for creating a variable.
It's possible to change the value of the variable later:
*/
let varCanChange = 'Now there is some text';

/*
We use const keyword when we don't want to change the value
of the variable in future:
*/
const CONST_EXAMPLE = 'You can\'t change this';

export { varCanChange, CONST_EXAMPLE }
